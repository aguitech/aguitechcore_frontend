# PDF Export

Generación de PDFs (cotizaciones, reportes, facturas).

## API
- `POST /api/pdf/quote` — genera PDF de cotización
- `POST /api/pdf/report` — genera reporte

Usa `pdfkit` o `puppeteer` (ver `server/src/controllers/pdfExport.controller.js`).

## Configuración
Pendiente. Ver issue #?.
